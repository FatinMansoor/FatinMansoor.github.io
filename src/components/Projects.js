const Projects = () => {
    return(
        <section id = "projects" className="projects">
            <h2>Projects</h2>
            <p>Currently, I'm working on my porfolio website. More projects are under development</p>
        </section>
    );
};

export default Projects;